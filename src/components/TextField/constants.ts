/**
 * Суффикс `testID` сообщения об ошибке: для поля `passport` ошибка
 * получает `passport-error`. Часть контракта компонента — по нему тесты
 * находят сообщение.
 */
export const ERROR_TEST_ID_SUFFIX = '-error';

/**
 * Оценка высоты строки для текста размера 16 — из неё считается
 * минимальная высота многострочного поля. Точное значение зависит от
 * шрифта и системного масштаба, но минимум и не должен быть точным: поле
 * растёт с содержимым.
 */
export const LINE_HEIGHT_ESTIMATE = 24;

/** Вертикальные отступы поля в сумме (8px сверху и снизу). */
export const INPUT_VERTICAL_PADDING = 16;
