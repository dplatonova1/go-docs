/**
 * Размеры, которые обязаны совпадать у нескольких компонентов.
 *
 * Константа одного компонента живёт в его `constants.ts`; сюда выносится
 * только то, что разошлось бы при правке в одном месте из нескольких.
 */

/**
 * Минимальный тач-таргет, ниже которого в элемент тяжело попасть.
 * Это не элемент оформления, менять при редизайне нельзя.
 */
export const MIN_TOUCH_TARGET = 44;

/** Скругления из темы: `--radius: 0.5rem` = 8, остальные от него ±2/4. */
export const RADII = {
  sm: 4,
  md: 6,
  lg: 8,
  xl: 12,
} as const;
