<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      INTERNET здесь намеренно НЕ объявлен: в фазах 0-5 приложение работает
      полностью офлайн (см. docs/adr/0002-local-first-storage.md и
      docs/adr/0004-offline-deterministic-form-parsing.md). Разрешение нужно
      только Metro в dev-сборках и объявлено в src/debug и src/debugOptimized.
      Возвращать в main следует только вместе с ADR на облачную синхронизацию.
    -->

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
